// Code generated by goa v3.0.9, DO NOT EDIT.
//
// starter HTTP client CLI support package
//
// Command:
// $ goa gen starter/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"
	starterc "starter/gen/http/starter/client"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `starter login-by-username
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` starter login-by-username --body '{
      "password": "password",
      "username": "user"
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		starterFlags = flag.NewFlagSet("starter", flag.ContinueOnError)

		starterLoginByUsernameFlags    = flag.NewFlagSet("login-by-username", flag.ExitOnError)
		starterLoginByUsernameBodyFlag = starterLoginByUsernameFlags.String("body", "REQUIRED", "")
	)
	starterFlags.Usage = starterUsage
	starterLoginByUsernameFlags.Usage = starterLoginByUsernameUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "starter":
			svcf = starterFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "starter":
			switch epn {
			case "login-by-username":
				epf = starterLoginByUsernameFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "starter":
			c := starterc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "login-by-username":
				endpoint = c.LoginByUsername()
				data, err = starterc.BuildLoginByUsernamePayload(*starterLoginByUsernameBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// starterUsage displays the usage of the starter command and its subcommands.
func starterUsage() {
	fmt.Fprintf(os.Stderr, `微服务
Usage:
    %s [globalflags] starter COMMAND [flags]

COMMAND:
    login-by-username: 使用账号密码登录

Additional help:
    %s starter COMMAND --help
`, os.Args[0], os.Args[0])
}
func starterLoginByUsernameUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] starter login-by-username -body JSON

使用账号密码登录
    -body JSON: 

Example:
    `+os.Args[0]+` starter login-by-username --body '{
      "password": "password",
      "username": "user"
   }'
`, os.Args[0])
}
